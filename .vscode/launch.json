{
    "configurations": [
        {
            "name": "Server",
            "type": "node",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**",
                "${workspaceFolder}/node_modules/**"
            ],
            "program": "${workspaceFolder}/bootstrap/server/dist/main.js",
            "cwd": "${workspaceFolder}/bootstrap/server",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}